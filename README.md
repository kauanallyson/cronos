# Cronos

um simples programa para marcar o tempo enquanto ouve musicas.

compilação:

```bash
cc -o nob nob.c # apenas 1 vez!

./nob # para compilar o cronos

./build/cronos # para usar o cronos!
```
